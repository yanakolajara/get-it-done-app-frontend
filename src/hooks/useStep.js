import React from "react";

function useStep(props) {
  React.useEffect(() => {}, []);
  return {
    // handleAddStep,
    // handleDeleteStep,
    // handleCompleteStep,
  };
}

export { useStep };
